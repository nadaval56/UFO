# Required for Hebrew translation of file descriptions via the Claude API.
# Skip translation with `python build_manifest.py --no-translate`.
anthropic>=0.39

# Phase 2 — uncomment when wiring PDF text extraction / OCR:
# pdfplumber>=0.10
# pytesseract>=0.3.10
# Pillow>=10.0
# tqdm>=4.66
